import React from "react";
import style from './Setting.module.css';

type SettingType = {}

const Setting: React.FC<SettingType> = (props) => {

    return (
        <div>
            Setting
        </div>
    );
}

export default Setting;